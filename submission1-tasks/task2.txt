{INPUT FILE "A.csv" AS a NOLABELS}
LET permutationDropMatch BE
    SELECT COLUMNS a.3, a.1
    FROM a
    WHERE a.1 = a.2 
    -
LET sortedPermutationDropMatch BE
    SORT permutationDropMatch
    -
QUERIESEND
OUTPUT sortedPermutationDropMatch TO STANDARD
